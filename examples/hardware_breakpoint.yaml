---
config:
  max_depht: 10
  solve_arguments: true
given:
  pattern:
    - call:
      - "@any"
      - "<ptrace@plt>"
      address-capture: ptrace-call
where:
  - a1 := argument(ptrace-call, 1)
  - a3 := argument(ptrace-call, 3)
  - analysis-end := ptrace-call
then:
  - some_solution(a1 == 3 and  a3 == 848)
report: "Debuggind evation trough hardware breakpoint detection"
