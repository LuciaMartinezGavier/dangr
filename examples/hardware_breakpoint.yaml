---
config:
  max_depht: 10
  solve_arguments: true
given:
  pattern:
    - call:
      - "@any"
      - "<ptrace@plt>"
      - anchore: ptrace_call
where:
  - $a1 := argument(ptrace_call, 1)
  - $a3 := argument(ptrace_call, 3)
then:
  - some_solution($a1 == 3 and  $a3 == 848)
report: "Debuggind evation trough hardware breakpoint detection"
