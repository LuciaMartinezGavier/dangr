---
config:
  max-depht: 10
  solve-arguments: true # default false
given:
    pattern: # structural pattern
    - add:
        - $deref:
            main_reg: "@any-dx"
        - "@any"
    - cmp
      - "@any-y"
      - "@any-z"
where: # symbolic constraints
  - $y -> $dx
such-that: # solver goals
  - $y = $z
then: # aritmetic constraints
  - $dx == 0xFA1E0FF3
report: "Debuggind evasion trough Software breakpoint detection"
