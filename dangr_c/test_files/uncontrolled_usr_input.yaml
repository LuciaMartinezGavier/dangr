---
given:
  pattern: 'mock uncontrolled_input_0078'
  #   - mov:
  #     - "$any"
  #     - "@deref":
  #       - main_reg: "$any-ptr"
where:
  - (_anyarg -> ptr)
such-that:
  - upper_unbounded(ptr)
report: "Pointer from user input is not bounded"
